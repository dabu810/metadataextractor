# Ontology Agent API service dependencies
# Lightweight — no DB connectors needed

# FastAPI service layer
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Ontology generation
rdflib>=7.0.0

# LangGraph pipeline
langgraph>=0.2.0
langchain-core>=0.3.0
